# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=earthmc
DB_USER=earthmc_worker
DB_PASSWORD=changeme
DB_POOL_MAX=10

# Cloud SQL (for Cloud Run — leave empty for local dev)
CLOUD_SQL_CONNECTION_NAME=

# Scraper intervals
HIGH_FREQ_INTERVAL=3s
LOW_FREQ_INTERVAL=3m

# Server
PORT=8080
